package JavaSessions;

public class StringReverseConcept {

	public static void main(String[] args) {
		// To reverse and print the string value.
		String str="Selenium";//muineleS
		char arr[] = str.toCharArray();
		// we should use the method call "toCharArray", 
		//which breaks the string to different characters, we can see this when mouse over the method.
	}

}
